use std::time::{Duration, Instant};

fn main() {
  var vec = Vec::new();
  for i in 0..1000000 {
    vec.push(i);
  }

  let start = Instant::now();
  for i in vec {
    i += 1;
  }

  println!("{:?}", start.elapsed()); 
}